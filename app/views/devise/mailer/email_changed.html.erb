<p>ITTO個別指導学院高坂駅前校塾内システムのメールアドレス変更確認メールです。</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>メールアドレスを<%= @resource.unconfirmed_email %>に変更中です。</p>
<% else %>
  <p>メールアドレスが　<%= @resource.email %>　に変更されました。</p>
<% end %>
