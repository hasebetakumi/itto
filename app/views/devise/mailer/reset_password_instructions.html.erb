<p>ITTO個別指導学院高坂駅前校塾内システムのパスワード変更メールです。</p>
<p>パスワードの変更要求を受け取りました。以下のリンクから変更を行います。</p>

<p><%= link_to 'パスワードを変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>リクエストしていない場合は、このメールを無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成しない限り、パスワードは変更されません。</p>
